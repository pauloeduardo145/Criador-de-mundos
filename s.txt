#INICIO

frame_inicio = ctk.CTkFrame(janela)
frame_inicio.grid(column=1,row=0, ipadx=100, ipady=90)


inicio = ctk.CTkLabel(frame_inicio,text="INICIO")
inicio.grid(column=1,row=0, sticky="NE")


frame_teste = ctk.CTkFrame(janela)
frame_teste.grid(column=0,row=1, sticky="n")

container = ctk.CTkLabel(frame_teste,text='container')
container.grid(column=0,row=1, padx=100,pady=90)